services:
  # Backend Web Service
  - type: web
    name: nineteen06-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index.cjs
    healthCheckPath: /
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production

  # Frontend Static Site
  - type: web
    name: nineteen06-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /api/*
        # Updated to match your successful backend deployment URL
        destination: https://nineteen06.onrender.com/api/*
      - type: rewrite
        source: /*
        destination: /index.html
